
Add sensor SDRs back into the main SDR repository (if they can from
there).

Support for multiple LAN connections to the same box in ipmi_lan.

Add support for FRU access.

Add support for the chassis commands.

Add LAN failure detection to the ipmi_lan.c code.

In ipmi_lan.c, add reconnecting when a LAN connection goes down.

Time LAN session in lanserv_ipmi.c

Add incoming sequence number handling (to make user the IPMI LAN
sequence numbers don't get too far out of line) to IPMI LAN.

Add platform event traps to the LAN code.
