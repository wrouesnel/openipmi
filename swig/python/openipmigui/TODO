Event Log interpretation
MC commands:
   send message
   SDR fetch and display
   pef
Domain
   connections
   fetch FRU
   send message
   pet - including the ability to save and restore them
snmp trap handling (saved as a stored configuration)
Debug settings (--dmem, --dmsg, etc)
command-line interface